package Csla.Validation;

/**
 * Values for validation rule severities.
 * 
 * 
 * @author Eric
 * @version 1.0
 * @created 21-Dec-2009 7:10:43 PM
 */
public enum RuleSeverity {
	/**
	 * Represents a serious business rule violation that should cause an object to be
	 * considered invalid.
	 * 
	 *        
	 */
	ERROR,
	/**
	 * Represents a business rule violation that should be displayed to the user, but
	 * which should not make an object be invalid.
	 * 
	 *        
	 */
	WARNING,
	/**
	 * Represents a business rule result that should be displayed to the user, but
	 * which is less severe than a warning.
	 * 
	 *        
	 */
	INFORMATION
}